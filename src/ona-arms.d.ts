import type {CharacterPack} from './schema.js';
/** Mutates an Ona spatial pack, preserving legacy shoulder animation and physics. */
export function addOnaArmJoints(pack:CharacterPack):CharacterPack;
