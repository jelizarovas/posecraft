import {EpisodeController} from './episode.js';
import {applyTake} from './performance.js';
let controller;
self.onmessage=({data:m})=>{try{if(m.type==='init'){controller=new EpisodeController(m.project);self.postMessage({type:'ready'});}else if(m.type==='frame'){const start=performance.now();self.postMessage({type:'frame',frame:controller.frame(m.time),computeMs:performance.now()-start});}else if(m.type==='bake')self.postMessage({type:'bake',id:m.id,keys:controller.bakeMotion(m.shot,m.actor)});else if(m.type==='performance'){const project=structuredClone(controller.project);applyTake(project,m.shot,m.actor,m.take,m.start);self.postMessage({type:'performance',id:m.id,cue:project.shots.find(s=>s.id===m.shot).actors[m.actor]});}}catch(error){self.postMessage({type:'error',message:error.message,id:m.id});}};
